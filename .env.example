# Neon Postgres connection string (optional locally — memory store used if unset)
DATABASE_URL=

# Local geo stub so two clients on localhost can match
DEV_GEO_CITY=LocalDev
DEV_GEO_REGION=Dev
DEV_GEO_COUNTRY=XX
DEV_GEO_LAT=0
DEV_GEO_LNG=0

# API port for local `npm run dev:api`
PORT=8787
